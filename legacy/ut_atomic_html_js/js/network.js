// Optional network stub — replace with your WebSocket logic
(function(){
  // No-op: demonstrate where you'd hook spawn/updates
  // window.dispatchEvent(new CustomEvent('net:spawn', {detail:{id:'me'}}));
})();
